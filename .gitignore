# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.mlops_env/

# Node (report generator deps)
node_modules/

# Notebooks
.ipynb_checkpoints/

# MLflow
mlruns/
mlartifacts/
mlflow.db

# Models (large binaries - keep only the final tracked artifact if needed)
models/*.pkl
models/*.joblib
models/*.onnx

# OS / IDE
.DS_Store
.idea/
.vscode/
docs/

# Recording (too large for git; published as a GitHub Release asset instead)
*.mp4
# Data (keep the download script; regenerate raw locally)
# Comment the next line out if your grader wants the CSV committed.
# data/heart_disease_raw.csv
